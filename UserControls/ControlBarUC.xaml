<UserControl
    x:Class="PatronGamingMonitor.UserControls.ControlBarUC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:PatronGamingMonitor.UserControls"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Name="ucControlBar"
    Background="{DynamicResource MaterialDesignPaper}"
    DataContext="{StaticResource ControlBarVM}"
    FontFamily="{DynamicResource MaterialDesignFont}"
    TextElement.FontSize="13"
    TextElement.FontWeight="Regular"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    TextOptions.TextFormattingMode="Ideal"
    TextOptions.TextRenderingMode="Auto"
    mc:Ignorable="d">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="MouseLeftButtonDown">
            <i:InvokeCommandAction Command="{Binding MouseMoveWindowCommand}"
                CommandParameter="{Binding ElementName=ucControlBar}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <materialDesign:ColorZone Mode="PrimaryMid">
            <DockPanel>
                <!-- Right StackPanel for Window Controls -->
                <StackPanel
                    Background="Transparent"
                    DockPanel.Dock="Right"
                    Orientation="Horizontal">
                    <StackPanel.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Margin" Value="0,5,5,5" />
                            <Setter Property="Width" Value="40" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="Foreground" Value="WhiteSmoke" />

                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="Black"
                                            BorderThickness="0">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                VerticalAlignment="Center" />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>

                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1A3650" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Resources>
                    <Button
                        BorderThickness="0"
                        Command="{Binding MinimizeCommand}"
                        CommandParameter="{Binding ElementName=ucControlBar}"
                        ToolBar.OverflowMode="AsNeeded"
                        ToolTip="Minimize" Click="Button_Click">
                        <materialDesign:PackIcon Kind="WindowMinimize" />
                    </Button>
                    <Button
                        BorderThickness="0"
                        Command="{Binding MaximizeCommand}"
                        CommandParameter="{Binding ElementName=ucControlBar}"
                        ToolBar.OverflowMode="AsNeeded"
                        ToolTip="Maximize"
                        Visibility="Collapsed">
                        <materialDesign:PackIcon Kind="WindowMaximize" />
                    </Button>
                    <Button
                        BorderThickness="0"
                        Command="{Binding CloseWindowCommand}"
                        CommandParameter="{Binding ElementName=ucControlBar}"
                        ToolBar.OverflowMode="AsNeeded"
                        ToolTip="Close">
                        <materialDesign:PackIcon Kind="WindowClose" />
                    </Button>
                </StackPanel>

                <!-- Left StackPanel for Logo and Title -->
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <!-- Image Logo -->
                    <Image
                        Source="pack://application:,,,/Assets/Images/NewLogo-01.png"
                        Width="80"
                        Height="60"
                        Margin="5,0,0,0"
                        VerticalAlignment="Center" />

                    <!-- TextBlock -->
                    <TextBlock
                        Style="{StaticResource DefaultTextBlockStyle}"
                        VerticalAlignment="Center"
                        Text="{Binding Tag, ElementName=ucControlBar}" />
                </StackPanel>
            </DockPanel>
        </materialDesign:ColorZone>
    </Grid>
</UserControl>